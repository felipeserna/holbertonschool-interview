#include "menger.h"
/**
* menger - Draws a 2D Menger Sponge
* @level: level of the Menger Sponge to draw
* Return: nothing
*/
void menger(int level)
{
	if (level < 0)
		return;

	if (level == 0)
		printf("#\n");
}
